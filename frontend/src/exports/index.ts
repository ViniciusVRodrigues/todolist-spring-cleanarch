/**
 * Module Federation Exports
 * 
 * This directory contains self-sufficient wrapper components
 * designed to be exposed via Module Federation.
 * Each export includes all necessary providers and dependencies
 * so it can work independently in a Host application.
 */

export { default as HomePageExport } from './HomePageExport';
